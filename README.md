# Msquic Echo

Small quic server that echoes using msquic. Useful for simple testing.

Supports:

- quic datagram packets 
- bidirectional streams (length prefixed message stream)
- (todo) unidirectional streams

ALPN "echo"
